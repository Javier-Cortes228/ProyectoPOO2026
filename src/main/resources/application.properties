spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/bandumusic}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=Jcag2288
spring.datasource.driver-class-name=org.postgresql.Driver

# Evita exponer consultas en consola por defecto; se puede activar localmente con JPA_SHOW_SQL=true.
spring.jpa.show-sql=${JPA_SHOW_SQL:false}

# En desarrollo se conserva update para no romper la demo; en producciÃ³n deberÃ­a usarse validate + migraciones.
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}

# Especifica el dialecto de la base de datos.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# OrÃ­genes permitidos para futuros frontends locales, como Vite/React.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}

# API oficial de YouTube Data API. La clave debe venir del entorno, no del codigo.
youtube.api.key=${YOUTUBE_API_KEY:}
youtube.search.max-results=${YOUTUBE_SEARCH_MAX_RESULTS:8}

spring.jpa.open-in-view=false
